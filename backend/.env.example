# Serper.dev Google Search API
SERPER_API_KEY=

# OpenAI (used by openai-agents / Runner)
OPENAI_API_KEY=

# Optional: where artifacts are written (default: <repo>/outputs)
# OUTPUT_DIR=/absolute/path/to/outputs
