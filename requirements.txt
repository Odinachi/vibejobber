# For Cloud Functions, use `cloud_functions/functions/requirements.txt` and deploy via Firebase
# (`cd cloud_functions && firebase deploy --only functions`).
#
# Root Python deps (e.g. local backend) continue to use `backend/requirements.txt`.
